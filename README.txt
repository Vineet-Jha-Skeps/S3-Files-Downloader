- Ensure Python is installed on your system.
- Make sure you're logged into AWS in your Chrome browser. By default, it should be logged in as it's connected to our work accounts.
- Open the project folder in VSCode or your preferred IDE.
 Open the Terminal.
- Run `pip install -r requirements.txt` to install the necessary dependencies.
- Run `python -m streamlit run app.py`.
- Follow the instructions on the UI
